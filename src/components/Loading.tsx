// src/components/Loading.tsx
import React from 'react';
import CustomLoadingAnimation from './KaleidoscopeMaterial';

const Loading: React.FC = () => {
  return <CustomLoadingAnimation />;
};

export default Loading;
